---
title: "Hermes Crew — 직무 특화 멀티 에이전트 워크스페이스"
subtitle: "사내망 웹에서 여러 AI 에이전트와 동시에 일하고, 주간보고는 자동으로"
author: "(발표자명)"
date: "2026-06-18"
---

<!-- 각 `---` 구분선이 슬라이드 1장. Marp / Slidev 로 변환 가능. -->

# Hermes Crew

## 직무 특화 멀티 에이전트 워크스페이스

SSH 터미널에 갇힌 1인용 AI 에이전트를
**사내망 웹에서 함께 일하는 에이전트 팀**으로

---

# 문제 — 지금 어떻게 쓰고 있나

회사 리눅스 서버에 AI 에이전트(Hermes)를 설치해 쓰는데:

- 사내 정책상 Telegram/Slack 같은 메신저를 못 붙인다
- 그래서 매번: **윈도우 PC → SSH → 리눅스 → `hermes` 커맨드 → 터미널 채팅**
- 에이전트를 여러 개 동시에 돌리니 **무엇이 무슨 작업 중인지 추적 불가**, 컨텍스트 스위칭 폭증
- 에이전트가 한 일을 모아 **주간보고로 정리하는 게 매번 수작업** → 귀찮고 시간 소모

> 강력한 에이전트를 갖고도, **쓰는 방식이 1990년대 터미널**에 묶여 있다.

---

# 해결 — Hermes Crew

브라우저로 접속하는 **멀티 에이전트 관제탑**

1. 사내망 웹 UI 로 접속 (SSH·커맨드 불필요)
2. **직무별 특화 에이전트**와 각각 독립 대화창에서 메신저처럼 대화
3. 사이드바에서 모든 에이전트의 **상태를 한눈에**
4. 리더 에이전트가 **주간보고를 자동 생성**

---

# 무엇을 새로 만드나 (해커톤 범위)

오픈소스 자산 위에 **직무 특화 레이어**를 얹는다 — 바퀴를 재발명하지 않는다.

| 레이어 | 출처 | 우리가 하는 일 |
|---|---|---|
| 에이전트 런타임 | Hermes Agent (NousResearch, MIT) | 그대로 사용 |
| 통신 | Hermes OpenAI 호환 API (:8642, 네이티브) | endpoint 연결 |
| **동시 멀티채팅 프론트** | — | **신규 구현 (프로필별 endpoint 병렬)** |
| **직무 특화 프로필 세트** | — | **신규 구성** |
| **주간보고 자동화** | — | **신규 구현** |
| **스킬/작업 Kanban** | Hermes kanban.db + 스킬 enable | **신규 시각화 (스킬 보드)** |
| 오케스트레이션 | Hermes delegate_task (네이티브) | config + UI 연결 |

> 핵심 기여 = "터미널 단일 세션" → "직무 에이전트 팀과의 동시 협업 + 보고 자동화 + 스킬 한눈에"

---

# 직무 특화 에이전트 팀

리눅스 디바이스 드라이버 엔지니어의 실제 업무에 매핑

- **driver** — 드라이버 설계·구현·디버깅
- **architect** — 코드 설계 (드라이버 + 에이전트 SW)
- **reviewer** — 패치 리뷰 (체크리스트 기반, 깐깐하게)
- **report** — 주간보고·발표자료 작성
- **brainstorm** — 아이디어 발산
- **lead** *(스트레치)* — 위 에이전트들을 지휘하는 오케스트레이터

각 에이전트 = 독립 메모리·세션·페르소나(SOUL.md). 오래 쓸수록 그 직무에 fit 된다.

---

# 아키텍처

```
[브라우저 · 경량 멀티채팅 프론트]   (현 mockup.html 확장)
        │  사내 LAN · OpenAI 호환 HTTP (API_SERVER_KEY)
        ▼
   ┌─ :8642  driver   프로필 ─┐
   ├─ :8643  reviewer 프로필 ─┤  각 프로필 = 자체 API 서버
   ├─ :8644  report   프로필 ─┤  /v1/chat/completions · /v1/models
   └─ :864x  lead     프로필 ─┘  (delegate_task 네이티브)
        │
        └ 스킬 · 상태 · kanban = hermes CLI / config 조회 (경량 백엔드)
```

- **통신**: Hermes **OpenAI 호환 API(:8642)** — 강결합/SSH호출 아님, 표준 HTTP (프론트·hermes 다른 머신도 가능)
- API 로 호출해도 **memory · skill · tool 그대로 유지** / 멀티 에이전트 = 프로필별 포트(=model)
- **오케스트레이션**: hermes 네이티브 `delegate_task`
- 프론트: 경량 자체 구현(현 mockup 확장) → fork 불필요, 1~2일 내 동작

---

# 화면 설계 ① — 멀티 에이전트 워크스페이스

```
┌─ Hermes Crew ───────────────────────── 🟢 5 agents ─ ⚙ 👤 ─┐
│ AGENTS        │  # driver · Linux Driver                    │
│               │ ─────────────────────────────────────────── │
│ 🟢 driver   ● │  driver       probe() NULL deref 의심…       │
│ 🟢 reviewer   │                                             │
│ 🟢 report  ②  │           me    그 경로 스택 떠줘 →          │
│ 🟡 architect  │  driver   (typing…)                          │
│ ⚪ brainstorm │ ─────────────────────────────────────────── │
│ + new agent   │  > 메시지…                          [send]   │
│ ───────────   │                                             │
│ [📋 주간보고] │                                             │
└───────────────┴─────────────────────────────────────────────┘
```

- **좌측 사이드바**: 에이전트 목록 + 상태(🟢run / 🟡idle / ⚪stop) · 안읽음 배지(②) · 활성(●)
- **우측 대화창**: 선택한 에이전트와 독립 대화 — 전환해도 각 세션·메모리 유지
- 슬랙 워크스페이스처럼: 여러 에이전트를 한 화면에서 오가며 동시 협업

---

# 화면 설계 ② — 주간보고 자동 생성 (킬러)

```
┌─ 📋 주간보고 · 2026-W25 ───────────── [복사] [닫기] ─┐
│  ## 완료                                              │
│   • [driver]    드라이버 probe 경로 디버깅            │
│   • [reviewer]  패치 3건 리뷰 (LGTM 2 / 보류 1)       │
│  ## 진행 중                                           │
│   • [architect] 에이전트 모듈 구조 재설계             │
│  ## Blocker                                           │
│   • [driver]    보드 리비전 대기                      │
│ ───────────────────────────────────────────────────  │
│  source: driver · reviewer · report 세션 자동 집계    │
└───────────────────────────────────────────────────────┘
```

버튼 한 번 → 각 에이전트 세션/메모리를 모아 **완료/진행/Blocker 분류 + 보고체 변환** → 마크다운 보고서

---

# 화면 설계 ③ — 오케스트레이션 (hermes 네이티브)

```
        me
         │  "패치 리뷰 + 드라이버 점검 동시에"
         ▼
     ┌────────┐
     │  lead  │ ──── 작업 분해 · 분배
     └───┬────┘
   ┌─────┼──────┐
   ▼     ▼      ▼
reviewer driver report
   └─────┴──────┘
         │  결과 취합
         ▼
       lead ───→ me   (통합 보고)
```

리더 에이전트가 하위 직무 에이전트에 작업을 분배하고 결과를 모아 한 번에 보고

---

# 화면 설계 ④ — 스킬 보드 (Kanban)

```
┌ driver ──────┐ ┌ reviewer ────┐ ┌ report ──────┐ ┌ architect ───┐
│ ▣ kernel-dbg │ │ ▣ diff-review│ │ ▣ weekly-rpt │ │ ▣ design-doc │
│ ▣ dmesg-scan │ │ ▣ checklist  │ │ ▣ summarize  │ │ ▣ tradeoff   │
│ ▣ gdb-trace  │ │ ▢ style-lint │ │ ▣ slide-gen  │ │ ▢ uml-draw   │
│ ▢ patch-test │ │              │ │              │ │              │
└──────────────┘ └──────────────┘ └──────────────┘ └──────────────┘
   ▣ enabled    ▢ available      ← 카드를 드래그해 에이전트에 장착/해제
```

- 컬럼 = 직무 에이전트, 카드 = 보유 스킬 → **누가 무슨 능력을 가졌는지 한눈에**
- 스킬 on/off · 에이전트 간 재배치를 보드에서 직접 (Hermes 의 프로필별 스킬 enable 네이티브 활용)
- **작업(task) 보드**도 같은 방식 — Hermes 프로필별 `kanban.db` 를 To do / Doing / Done 으로 시각화

---

# 데모 시나리오

1. 윈도우 브라우저로 사내 서버 접속 — SSH 없이
2. 사이드바: `driver` `reviewer` `report` 에이전트 + 상태 초록불
3. `driver` 와 드라이버 디버깅 대화 → 동시에 `reviewer` 와 패치 리뷰 대화
4. **(킬러)** `report` 에게 "이번 주 한 일 주간보고" → 세션 집계 → 완성된 보고서 출력
5. `lead` 에게 "리뷰 + 점검 동시에" → 하위 에이전트에 분배 → 취합 *(hermes 네이티브 delegate_task)*
6. **스킬 보드**로 각 에이전트가 가진 능력을 한눈에 + on/off

---

# 차별점 / 임팩트

- **즉시 체감**: 매일 겪는 "SSH→커맨드" 불편을 브라우저 한 번으로 제거
- **재발명 없음**: 검증된 오픈소스 위에 직무 특화만 얹어 1~2일에 동작
- **확장성**: 직무 프로필은 누구나 자기 업무에 맞게 추가 가능 (드라이버 팀 → QA 팀 → …)
- **킬러 기능**: 주간보고 자동화 — 가장 귀찮은 일을 에이전트 팀이 대신

---

# 일정 (1~2일)

- **Day 1 오전** — 환경 준비 + 사내망 웹 접속 검증
- **Day 1 오후** — 직무 특화 프로필 3~5개 구성
- **Day 1 밤~Day 2 오전** — 동시 멀티채팅 UI 개조 (핵심 기여)
- **Day 2 오전** — 주간보고 자동 생성
- **Day 2 오후** — 리허설 + 발표

각 단계마다 **폴백**을 둬서, 막혀도 데모는 항상 성립.

---

# 향후 방향

- 오케스트레이션 고도화 — 리더가 복합 작업을 자동 분해·분배
- 직무 프로필 템플릿화 — 팀 단위 배포 ("우리 팀 에이전트 팩")
- 패치 리뷰 / 디버깅 워크플로우를 재사용 skill 로 축적 (Hermes skill 자가생성 활용)

---

# 감사합니다

**Hermes Crew** — 터미널에 갇힌 에이전트를, 함께 일하는 팀으로.
