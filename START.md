# START — 해커톤 당일 시작 가이드

> 내일 이 폴더에서 `claude` 를 실행한 뒤, 아래 프롬프트를 순서대로 복붙해서 진행한다.
> (CLAUDE.md 는 자동 로드되지만 SPEC.md 는 명시적으로 읽으라고 해야 안전)

---

## ✅ Claude Code 열기 전 — 손으로 먼저

- [ ] `.env.example` → `.env` 복사 후 사내값 채우기 (서버 IP·`API_SERVER_KEY`)
- [ ] `agents.config.example.json` → `agents.config.json` 복사 후 실제 프로필명·포트 기입
- [ ] 이 폴더에서 터미널 열고 `claude` 실행

---

## ⭐ 1) 메인 시작 프롬프트 (그대로 복붙)

```
이 폴더는 내일 발표할 해커톤 프로젝트야. 먼저 CLAUDE.md 와 SPEC.md 를 정독해줘.
그다음 .env 와 agents.config.json 이 실제값으로 채워졌는지 확인해줘 (비어 있으면 알려줘).
준비됐으면 TODO.md 의 Phase 2(동시 멀티채팅 프론트)부터 구현을 시작하자.

규칙:
- Phase 0~1(사내 서버 프로필 생성·API 서버 기동)은 내가 직접 할 거야.
  너는 실행할 명령만 안내하고, 내가 결과를 붙여주면 검증만 해줘.
- 구현은 SPEC.md 의 엔드포인트 계약과 호출 규격(Authorization: Bearer,
  stream SSE, messages 누적, X-Hermes-Session-Key)을 정확히 따라줘.
- 한 기능에 매몰되지 말고, 막히면 멈추고 TODO 의 폴백으로 전환할지 나한테 물어봐.
```

---

## 🔧 2) 서버 환경 셋업 안내가 필요할 때 (Phase 0~1)

```
사내 서버에서 driver / reviewer / report 프로필을 만들고, 각자 다른 포트로
API 서버를 동시에 띄우려고 해. SPEC.md §6 기준으로 내가 순서대로 실행할
명령을 알려줘. 실행은 내가 직접 한다.
```

검증할 때:
```
방금 명령 실행 결과야. 정상인지 / 다음 단계로 가도 되는지 봐줘:
<여기에 curl·터미널 출력 붙여넣기>
```

---

## 📋 3) 주간보고 기능 (Phase 3)

```
Phase 3 주간보고를 구현하자. SPEC.md §7 그대로:
POST /api/report 가 report 프로필에 고정 프롬프트를 보내 완료/진행/Blocker
마크다운을 받아오고, 프론트 "주간보고 생성" 버튼이 그 결과를 모달에 렌더하게.
```

---

## 🎬 4) 데모 직전 점검 (Phase 4)

```
이제 데모 리허설이야. CLAUDE.md 의 "데모 성공 기준" 5단계를 처음부터 끝까지
한 번 같이 점검하자. 끊기거나 느린 지점이 있으면 미리 워밍업할 방법도 알려줘.
```

---

## 🆘 막힐 때 (폴백 전환)

```
이 부분이 계속 안 풀려서 시간이 너무 걸려. TODO 의 폴백으로 전환하는 게
맞을까? 데모를 성립시키는 가장 안전한 차선을 제안해줘.
```

---

## 기억할 것

- **데모 우선** — 완성도보다 "보이는 흐름" 먼저.
- ⚠️ 당일 검증 3건(SPEC §0): delegate_task API / kanban 스키마 / 스킬 토글 반영 시점 — 안 되면 폴백 있으니 당황 금지.
- 막히면 30분 룰: 한 곳에 30분 이상 매몰되면 폴백.
