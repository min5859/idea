# HermesTalk — 멀티 에이전트 메신저

> 여러 도메인 특화 Hermes 에이전트를 메신저처럼 관리하고,
> "그룹방"에 리더 에이전트를 넣으면 서브 에이전트들에게 작업을 위임·취합해 답하는 셀프호스팅 관제 메신저.

---

## 1. 문제 (Problem)

사내 보안 환경에서 Hermes Agent를 서버에 셀프호스팅해 쓰고 있다. 그런데:

- 텔레그램 같은 외부 메신저를 못 쓴다 → 항상 **Windows PC → SSH → 리눅스 서버 → `hermes` 커맨드 → 터미널 채팅**.
- 에이전트가 **하나뿐**이라 드라이버 코드, 설계, 리뷰, 주간보고를 한 에이전트가 다 처리 → 맥락이 섞이고 특화가 안 됨.
- 여러 작업을 **누군가 지휘해서 모아주는** 흐름이 없다. 사람이 일일이 시킨다.

## 2. 솔루션 (Solution)

**HermesTalk** = 셀프호스팅 웹 메신저 + 멀티 에이전트 관제. 좌측 레일로 **채팅 · 스킬 보드 · 작업 칸반** 3개 뷰를 오간다.

- **💬 채팅방 목록**: 카톡처럼, 각 방이 도메인 특화 Hermes 에이전트.
- **🧩 그룹방 오케스트레이션**: 그룹방에 메시지를 넣으면 **팀리더봇**이 적절한 서브 에이전트에게 위임하고, 각 봇의 작업이 말풍선으로 실시간 표시되며, 리더가 최종 취합 답변을 낸다.
- **🗂 스킬 보드**: 열=에이전트, 카드=그 에이전트가 가진 skill. 누가 무엇을 할 수 있는지 한눈에 (read-only 카탈로그).
- **📋 작업 칸반**: Backlog/To Do/In Progress/Review/Done 레인. 위임된 작업이 카드로 흐르고 에이전트가 claim. (로컬 보드, 그룹방 위임과 연동)
- **🔒 셀프호스팅**: 외부 메신저 불필요. 사내망 브라우저에서 바로 접속.

## 3. 특화 에이전트 라인업 (실제 업무 기반)

| 에이전트 | 담당 업무 |
|---|---|
| 🐧 **드라이버봇** | 리눅스 디바이스 드라이버 코드 |
| 📐 **설계봇** | 코드/에이전트 SW 아키텍처 설계 |
| ⌨️ **구현봇** | 코드 구현 |
| 🔍 **리뷰봇** | 패치 리뷰 |
| 🐞 **디버그봇** | 디버깅 |
| 📊 **주간보고봇** | 주간보고 작성 |
| 🎤 **발표봇** | 발표자료 작성 |
| 👑 **팀리더봇** | (그룹방) 위 에이전트들을 지휘·취합 |

## 4. 차별점 (Why us)

이미 존재하는 것과 한 줄로 구분된다:

- `hermes-webui`(기존 OSS): **단일 에이전트 1:1 채팅**.
- `hermes-workspace`(Studio): 칸반·스킬매니저·inspector를 갖췄지만 **워크스페이스/Kanban 중심**.
- **HermesTalk: 메신저를 중심으로 채팅·스킬·칸반이 하나로 묶인 UX** — 대화하듯 지휘하고, 옆 탭에서 누가 뭘 할 수 있고 뭘 하고 있는지 본다.

핵심 차별점은 "기능 유무"가 아니라 **메신저 UX**다. Workspace에도 칸반·스킬이 있지만, HermesTalk는 *채팅이 1급 시민*이고 보드는 그 보조다.

### 연동 방식 비교 (우리 = 견고한 쪽)

| | 연동 방식 | 결합도 |
|---|---|---|
| `hermes-webui` | Hermes 내부 모듈 직접 import + `~/.hermes/` 공유 | **강결합** — 같은 호스트·버전 동기화 필요, Python에 묶임 |
| `hermes-workspace`(Studio) | OpenAI 호환 HTTP API (`webapi` :8642) | 느슨한 결합 |
| **HermesTalk** | **OpenAI 호환 HTTP API (:8642)** | **느슨한 결합** — 언어 자유(Next.js), 버전 비결합, 원격 접속 가능 |

우리는 Studio와 같은 **HTTP API 진영**이라 Hermes 내부 변경에 안 깨지고, 사내 서버의 Hermes에 별도 웹앱이 네트워크로 붙는다. (webui식 내부 직결이 아님)

추가로, 범용 함대 관제 OSS(`mission-control`, `TeamHero`)와 달리 **Hermes에 특화** — 프로필(특화 에이전트), sub-agent 위임, 스킬 API(`/v1/skills`), 영속 메모리를 그대로 활용한다.

## 5. 아키텍처 (한눈에)

```
[브라우저]         ── 메신저 UI (채팅방 목록 · 1:1 · 그룹방)
        │  SSE 스트리밍
        ▼
[HermesTalk 서버]  ── Next.js (UI + 프록시 API)
        │            · 에이전트 레지스트리(프로필 매핑)
        │            · /api/chat → Hermes API 서버 호출
        │  HTTP/SSE (OpenAI 호환)
        ▼
[Hermes 게이트웨이]── 프로필 1개 = 게이트웨이 1개 = 포트 1개 (model 필드는 장식용)
        │  :8642  POST /v1/runs → run_id → GET /v1/runs/{id}/events (토큰+위임 이벤트)
        │         GET /v1/skills (스킬 목록)   · Kanban API는 없음 → 칸반은 로컬
        ▼
[Hermes Agent]     ── 프로필별 특화 · sub-agent 위임 · 메모리
        (real로 띄운 프로필만 연동, 나머지는 mock)
```

핵심: **HermesTalk는 Hermes를 새로 만들지 않는다.** Hermes 내장 **API 서버(OpenAI 호환, :8642)**에 붙어 프로필·서브에이전트·위임 이벤트를 **메신저 UI로 노출**할 뿐이라 하루 안에 실동작 가능. (SSH·메신저 게이트웨이 불필요)

## 화면 설계 (와이어프레임)

### 전체 레이아웃 (사이드바 = 채팅방 목록 / 메인 = 채팅뷰)
```
┌──────────────────────────────────────────────────────────────┐
│  HermesTalk                                  👤 wooki   ⚙️      │
├────────────────────┬───────────────────────────────────────────┤
│ 🔍 검색…            │  👑 팀리더봇 (그룹방)              ● 작업중  │
│ ┌────────────────┐ │ ───────────────────────────────────────── │
│ │👑 팀리더봇 ●작업│ │                      ┌──────────────────┐  │
│ │ 리뷰 위임 중…   │ │                      │ 패치 리뷰하고     │나│
│ │🐧 드라이버봇 ●  │ │                      │ 주간보고 반영해줘 │  │
│ │ 패치 확인했어요 │ │                      └──────────────────┘  │
│ │🔍 리뷰봇   ●작업│ │  ┌─👑 팀리더봇──────────┐                  │
│ │ 3건 지적사항…   │ │  │ 리뷰봇·주간보고봇에   │                  │
│ │📊 주간보고봇 ○  │ │  │ 위임할게요.          │                  │
│ │ 반영 완료       │ │  └──────────────────────┘                  │
│ │🐞 디버그봇  ○   │ │  ╭┈🔍 리뷰봇 입력 중…┈╮ ← 위임 말풍선(점선) │
│ │📐 설계봇    ○   │ │  ╰┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈╯                    │
│ └────────────────┘ │  ┌┈🔍 리뷰봇┈┈┈┈┈┈┈┈┈┈┐                    │
│  ● 온라인 ○ 유휴   │  │ 락 해제 누락 1건…    │                    │
│                    │ ───────────────────────────────────────── │
│                    │  ┌──────────────────────────┐  ┌────┐      │
│                    │  │ 메시지 입력…             │  │전송│      │
│                    │  └──────────────────────────┘  └────┘      │
└────────────────────┴───────────────────────────────────────────┘
```

### 그룹방 위임 흐름 (★데모 하이라이트)
```
 [나] "패치 리뷰 → 주간보고 반영"
   │
   ▼
 ┌────────────┐  위임 (GET /v1/runs/{id}/events)
 │ 👑 팀리더봇 │ ────────┬───────────────┐
 └────────────┘         │               │
      ▲                 ▼               ▼
      │ 취합     ╭┈┈┈┈┈┈┈┈╮   ╭┈┈┈┈┈┈┈┈┈┈┈╮
      │          │🔍 리뷰봇 │   │📊 주간보고봇│ ← 회색 점선 말풍선
      └──────────│ 입력 중… │   │ 대기       │   (아바타 + 상태)
                 ╰┈┈┈┈┈┈┈┈╯   ╰┈┈┈┈┈┈┈┈┈┈┈╯
                     │               │
                  결과 말풍선      결과 말풍선
```
일반 말풍선(실선)과 위임 말풍선(점선·연한 배경)을 구분해, 한 화면에서 **리더 위임 → 서브봇 진행 → 리더 취합**이 시간순으로 흐른다.

좌측 레일(💬 채팅 / 🗂 스킬 / 📋 칸반)로 아래 두 뷰를 전환한다.

### 스킬 보드 (열=에이전트, 카드=skill)
```
🗂 스킬 보드
┌──────────────┬──────────────┬──────────────┬──────────────┐
│ 🐧 드라이버봇 │ 🔍 리뷰봇     │ 🐞 디버그봇   │ 📊 주간보고봇 │
├──────────────┼──────────────┼──────────────┼──────────────┤
│ ┌──────────┐ │ ┌──────────┐ │ ┌──────────┐ │ ┌──────────┐ │
│ │ kbuild   │ │ │patch-rev │ │ │ kgdb     │ │ │weekly-rep│ │
│ │ 드라이버  │ │ │ 패치 리뷰 │ │ │ 커널 디버그│ │ │ 주간보고  │ │
│ └──────────┘ │ └──────────┘ │ └──────────┘ │ └──────────┘ │
│ ┌──────────┐ │ ┌──────────┐ │ ┌──────────┐ │              │
│ │ dts-edit │ │ │checkpatch│ │ │ ftrace   │ │              │
│ └──────────┘ │ └──────────┘ │ └──────────┘ │              │
└──────────────┴──────────────┴──────────────┴──────────────┘
  "누가 무엇을 할 수 있는가"를 한눈에. (read-only / API 우선, 없으면 config)
```

### 작업 칸반 (위임 작업이 레인을 따라 흐름)
```
📋 작업 칸반
┌─────────┬─────────┬─────────────┬─────────┬─────────┐
│ Backlog │  To Do  │ In Progress │ Review  │  Done   │
├─────────┼─────────┼─────────────┼─────────┼─────────┤
│┌───────┐│┌───────┐│┌───────────┐│┌───────┐│┌───────┐│
││문서화 ││││드라이버││││패치 리뷰  ││││주간보고││││빌드   ││
││       ││││구현    ││││🔍리뷰봇   ││││📊보고봇││││✅완료 ││
│└───────┘│└───────┘│└───────────┘│└───────┘│└───────┘│
│         │         │ ← drag 이동 / 에이전트가 claim       │
└─────────┴─────────┴─────────────┴─────────┴─────────┘
  그룹방에서 위임된 작업이 카드로 생성되어 레인을 따라 이동.
  (드래그는 로컬 store에서 동작 — API 서버엔 Kanban 엔드포인트가 없어 로컬 전용)
```

## 6. 데모 시나리오 (심사 3분)

1. 채팅방 목록에 드라이버봇·DB/디버그봇·리뷰봇·팀리더봇이 떠 있음(presence 뱃지).
2. **1:1**: 디버그봇에게 "이 커널 패닉 로그 원인 분석" → 답변 스트리밍.
3. **그룹방**: "어제 올린 드라이버 패치 리뷰하고 주간보고에 반영해줘" 입력
   → 팀리더봇이 **리뷰봇 → 주간보고봇**에 위임하는 과정이 말풍선으로 실시간 표시
   → 리더가 취합한 최종 결과. ← **이 한 장면이 차별점 전부를 증명.**

## 7. MVP 범위 (해커톤 1일)

- **Must**: 채팅방 목록 + presence / 1:1 SSE 스트리밍 / 그룹방 위임 시각화 / 스킬 보드(read-only) / 작업 칸반(드래그·claim).
- **Nice**: real 에이전트 1~2개 실연동(1:1 + `/v1/skills`) / 토큰·큐 미니 대시보드.
- **제외**: 메모리 편집·파일 워크스페이스·reasoning inspector(기존 webui·workspace가 이미 함).

> 스코프 주의: 보드 2종을 더했으므로 **메신저 3기능이 항상 1순위**. 칸반은 로컬 store로 풀동작(드래그·claim) — API 서버엔 Kanban이 없어 로컬 전용이다(데모가 API에 안 묶임).

## 8. 향후 (Roadmap)

- 에이전트 함대 관측 대시보드(헬스/메모리/토큰 비용).
- 멀티 게이트웨이 연동(Hermes의 Telegram/Slack 게이트웨이를 방으로 흡수).
- 위임 정책/권한 관리(어떤 봇이 어떤 작업을 받을지 규칙화).
